{% extends 'base.html.twig' %}

{% block title %}Voir l'article{% endblock %}

{% block body %}
	<div class="container">
		<div class="my-5">
			<h1>Voir l'article</h1>
		</div>
        <div class="card">
            <img src="" class="card-img-top" alt="">
            <div class="card-body">
                <h3 class="card-title">{{ post.title }}</h3>
                <h5 class="card-title">{{ post.subtitle }}</h5>
                <p class="card-text">{{ post.content }}</p>
                <p>Date de création : {{ post.createdAt ? post.createdAt|date('d-m-Y H:i:s') : '' }}</p>
                <div class="d-flex justify-content-between">
                    <button class="btn btn-primary"><a href="{{ path('app_blog_edit', {"slug": post.slug}) }}" class="nav-link text-white">modifier</a></button>
                    {{ include('blog/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
{% endblock %}
