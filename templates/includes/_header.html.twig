<nav class="navbar navbar-expand-lg" style="background-color: #2e3b1f;">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active text-white px-2" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white px-2" href="{{ path('app_articles') }}">Blog</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white px-2" href="#">Contact</a>
                </li>
                {% if is_granted("ROLE_BLOG_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link text-white px-2" href="{{ path('app_blog_index') }}">Articles publiés</a>
                </li>
                {% endif %}                
                {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link text-white px-2" href="{{ path('app_admin_index') }}">Admin</a>
                </li>
                {% endif %}                
            </ul>
            {% if app.user %}
                <a href="{{ path('app_profile_index') }}" class="nav-link text-white px-2">Mon compte</a>
                <a href="{{ path('app_logout') }}" class="nav-link text-white px-2">Se déconnecter</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="nav-link text-white px-2">Se connecter</a>
                <a href="{{ path('app_register') }}" class="nav-link text-white px-2">Créer un compte</a>        
            {% endif %}
        </div>
    </div>
</nav>